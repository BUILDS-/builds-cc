---
interface Props {
  className: string;
  direction: "left" | "right";
  gap: number;
  speed: number;
}
const { className, ...rest } = Astro.props;
---

<div class=`${className} marquee` {...rest}>hi</div>

<script></script>

<style>
  .marquee {
    overflow: hidden;
    width: 100%;
    height: 60px;
    border-bottom: solid 1px var(--color-border);
  }
</style>
